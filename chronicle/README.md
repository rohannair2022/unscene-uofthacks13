# Chronicle

A Vite + React app.

## Run locally

```bash
npm install
npm run dev
```

## Notes

- This repo no longer depends on Base44 (the SDK + Vite plugin were removed).
- Data is stored locally in your browser via `localStorage` (see `src/api/base44Client.js`).
